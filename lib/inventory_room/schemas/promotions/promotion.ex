defmodule InventoryRoom.Promotions.Promotion do
  # import Ecto.Schema
  
  # schema "promotions" do
    # field(:description, :string) -- should be text in migration
    # field(:expires_at, :naive_datetime)
    # field(:starts_at, :naive_datetime)
    # field(:name, :string)
    # field(:type, :string)
    # field(:usage_limit, :integer)
    # field(:match_policy, :string) -- should default to all
    # field(:code, :string)
    # field(:advertise, :boolean) -- should default to false
    # field(:path, :string)
    # has_one(:promotion_category, PromotionCategory)
    # field(:per_code_usage_limit, :integer)
    # field(:apply_automatically, :boolean) -- should default to false
    # timestamps()
  # end
end