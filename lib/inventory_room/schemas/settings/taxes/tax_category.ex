defmodule InventoryRoom.Settings.Taxes.TaxCategory do
  # import Ecto.Schema

  # schema "tax_categories" do
    # field :name, :string
    # field :description, :string -- should be text
    # field :is_default, :boolean -- shuold default to false
    # field :tax_code, :string
    # timestamps()
  # end
end