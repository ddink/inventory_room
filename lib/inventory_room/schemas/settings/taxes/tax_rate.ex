defmodule InventoryRoom.Settings.Taxes.TaxRate do
  # import Ecto.Schema
  
  # schema "tax_rates" do
    # field :amount, :float
    # belongs_to :zone, Zone
    # has_many :tax_categories, TaxCategory
    # field :included_in_price, :boolean -- should default to false
    # field :name, :string
    # field :show_rate_in_label,  :boolean -- should deafult to true
    # field :deleted_at, :naive_datetime
    # timestamps()
  # end
end