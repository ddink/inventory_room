defmodule InventoryRoom.Settings.Store do
  # import Ecto.Schema

  # schema "store_settings" do
    # field :name, :string
    # field :url, :string
    # field :meta_description, :string -- should be text
    # field :meta_keywords, :string -- should be text
    # field :seo_title, :string
    # field :mail_from_address, :string
    # field :default_currency, :string
    # field :currency, :string
    # field :default, :boolean -- should default to true, required (cannot be nil)
    # field :cart_tax_country_iso, :string
    # timestamps()  
  # end
end