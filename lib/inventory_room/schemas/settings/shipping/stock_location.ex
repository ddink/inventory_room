defmodule InventoryRoom.Settings.Shipping.StockLocation do
  # import Ecto.Schema

  # schema "stock_locations" do
    # field :name, :string
    # field :default, :boolean -- should default to false, required (cannot be nil)
    # field :address1, :string
    # field :address2, :string
    # field :city, :string
    # field :address1, :string
    # field :address1, :string
    # has_one :state, State
    # has_one :country, Country
    # field :zipcode, :string
    # field :phone, :string
    # field :active, :boolean -- should default to true
    # field :backorderable_default, :boolean -- should default to false
    # field :propagate_all_variants, :boolean -- should default to true
    # field :internal_name, :string
    # field :position, :integer -- should default to 0
    # field :restock_inventory, :boolean -- should default to true, required (cannot be nil)
    # field :fulfillable, :boolean -- should default to true, required (cannot be nil)
    # field :code, :string
    # field :check_stock_on_transfer, :boolean -- should default to true
    # timestamps()
  # end
end