defmodule InventoryRoom.Settings.RefundsAndReturns.RefundReason do
  # import Ecto.Schema

  # schema "refund_reasons" do
    # field :name, :string
    # field :active, :boolean -- should default to true
    # field :mutable, :boolean -- should default to true
    # field :code, :string
    # timestamps()  
  # end
end