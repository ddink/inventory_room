defmodule InventoryRoom.Settings.RefundsAndReturns.ReimbursementType do
  # import Ecto.Schema

  # schema "reimbursement_types" do
    # field :name, :string
    # field :active, :boolean -- should default to true
    # field :mutable, :boolean -- shuould default to true
    # field :type, :string
    # timestamps()
  # end
end