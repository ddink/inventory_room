defmodule InventoryRoom.Products.OptionType do
  # import Ecto.Schema
	
	# schema "order_types" do
		# field(:name, :string) -- can limit to 100
		# field(:presentation, :string) -- can limit to 100
		# field :position, :integer -- should default to 0, required (cannot be nil)
		# timestamps()
	# end  
end